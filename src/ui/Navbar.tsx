import { FC } from 'react';

const Navbar: FC = () => {
  return (
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        // Add more navigation links here
      </ul>
    </nav>
  );
};

export default Navbar;