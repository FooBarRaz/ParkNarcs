import ImageUploadForm from '../../components/ImageUploadForm.server';

const Home = () => {
  return (
    <div>
      <h1>Upload Image</h1>
      <ImageUploadForm />
    </div>
  );
};

export default Home;
